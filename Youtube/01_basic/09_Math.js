// console.log(Math);

console.log(Math.abs(-6)) // it will change negative sign to positive

console.log(Math.round(5.6));// it will do round of same as school

console.log(Math.ceil(3.3)); // it will plus one value if it has value before point

console.log(Math.floor(3.3)); // it will removes values after point

// mostly we use math.random()

console.log(Math.random())

// Math.random () will give value in points and value start form 0.0 to 0.9 

let min = 10 
let max = 20
console.log(Math.floor(Math.random()*(max - min + 1) + min))